"""Spectator tooling: opt-in per-turn streaming + read-side parsers for the
Arena Spectator dashboard/replay server (scripts/foedus_spectator.py).

Nothing here is imported by the core engine (foedus/core.py, press.py,
resolve.py) -- this package is a read/observe-only layer on top of it.
"""
